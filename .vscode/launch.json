{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9005,
            "pathMappings": {
                "/media/sf_LegacySites/sopan": "${workspaceFolder}"
            }
        }
    ]
}